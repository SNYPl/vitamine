import React from "react";
import style from "./style.module.scss";

const Profile: React.FC = ({}) => {
  return <section className={`${style.profile}`}>Coming soon...</section>;
};

export default Profile;
