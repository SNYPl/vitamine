import React from "react";
import style from "./style.module.scss";

const Wishlist: React.FC = ({}) => {
  return <section className={`${style.wishlist}`}>Coming Soon..</section>;
};

export default Wishlist;
